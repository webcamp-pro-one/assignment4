<div class-"container">
    <div class="row">
        <h1>商品一覧</h1>
    </div> 
    <div class="">
        
        <% @items.each do |item| %>
        <div class="col">
        <%= link_to "image", item_path(item) %> 
        <%= item.name %></br>
        <%= item.price %>
        
        <% end %>
        </div>
    </div>
</div>